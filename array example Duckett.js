// Code Examples from Treehouse and Books I found

//creating an array  pg 71 Duckett

var colors;
colors = ['white', 'black', 'custom']; //creation of array literal 

var el = document.getElementById('colors');
el.text.Content = colors[0];

var colors = newArray('white',  //using constructor format for array creation
                       'black',
                       'custom');

var el = document. getElementById('colors');
el.textContext = colors[0];
